import type { SxProps } from '@mui/material';

export const googleContentSx: SxProps = {
    marginTop: 1,
    display: 'flex',
    alignItems: 'center',
    flexDirection: 'column',
};

export const googleFontSx: SxProps = {
    mt: 1,
    color: '#757575',
};

export const googleButtonSx: SxProps = {
    display: 'flex',
    justifyContent: 'center',
};
