import type { SxProps } from '@mui/material';

export const footerSx: SxProps = {
    top: 'auto',
    bottom: 0,
    height: '5.6rem',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 'auto',
    position: 'static',
};
